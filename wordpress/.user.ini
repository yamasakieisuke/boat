; /web/boat/api/.user.ini
; heteml の PHP (CGI/FPM) では Apache SetEnv が $_SERVER / $_ENV に反映されない。
; .user.ini の env[] ディレクティブで直接注入する。
; .htaccess SetEnv と同値を保つこと。
env[BOAT_SYNC_TOKEN] = dslGr00chvVut1fzLEEOnyoBnjAU
